%section#top-header.row
  %h1.col-md-2
    = link_to image_tag('/tmp/yelp_logo.png', width: 100, height: 50), home_path
  %ul.col-md-4.clearfix
    %li= link_to "Add a Business", new_business_path
  %form.col-md-5.navbar-form(action="")
    .form-group
      %input.form-control(type="text" placeholder="Search for videos here")
    %button.btn.btn-default(type="submit") Search
  #user_links.pull-right
    %ul
      %li.dropdown
        - if current_user
          %a(href="#" id="dlabel" role="button" data-toggle="dropdown" class="dropdown-toggle" data-target="#")
            = "Welcome, #{current_user.full_name}"
            %b.caret
          %ul.dropdown-menu(role="menu" aria-labelledby="dlabel")
            %li
              = link_to "Account", user_path(current_user)
              = link_to "Sign Out", sign_out_path
        - else
          = link_to "Sign In", sign_in_path, class: 'btn btn-default'
          = link_to "Register", register_path, class: 'btn btn-default'